# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project is pinned to SDK 54 for compatibility. Do not upgrade the `expo` or
`react-native` versions in `package.json` without being asked.
